# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric / Minecraft versions — bump together. See https://fabricmc.net/develop
# Pinned to 1.21.1 (not the latest 1.21.x) so this mod can share a server with Immersive Portals,
# whose newest published build (v6.0.6) only declares support up to 1.21.1.
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.16.2
fabric_version=0.109.0+1.21.1

# Mod properties
mod_version=0.1.0
maven_group=earth.tellus
archives_base_name=geoid
